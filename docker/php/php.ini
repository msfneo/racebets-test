; Money is handled as integer minor units and parsed from strings, never from
; floats, so PHP's float precision defaults are deliberately left untouched.

date.timezone = UTC

display_errors = Off
log_errors = On
error_log = /proc/self/fd/2
error_reporting = E_ALL

memory_limit = 256M
